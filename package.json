{
  "name": "creating-your-first-mcp-server",
  "version": "1.0.0",
  "description": "A production-ready TypeScript MCP server implementation with example tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "example": "ts-node examples/client.ts"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "typescript",
    "ai",
    "tools"
  ],
  "author": "ry-ops",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
